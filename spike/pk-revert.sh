#!/bin/bash

source ${PWD}/share.sh

# =============================================================================

cd ${PK_REPO}
git reset HEAD
git checkout .
git clean --force -d 

# =============================================================================
