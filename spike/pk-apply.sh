#!/bin/bash

source ${PWD}/share.sh

# =============================================================================

cd ${PK_REPO}

if [ -s ${PK_PATCH} ] ; then
  git apply ${PK_PATCH}
  git add --all
fi

# =============================================================================
